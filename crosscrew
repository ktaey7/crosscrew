#!/bin/sh
set -eu
# Resolve installed symlink without relying on platform-specific readlink -f.
exec python3 "$(python3 -c 'import pathlib,sys; print(pathlib.Path(sys.argv[1]).resolve().with_name("crosscrew.py"))' "$0")" "$@"
